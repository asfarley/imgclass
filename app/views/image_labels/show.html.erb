<p id="notice"><%= notice %></p>

<script type="text/javascript">
  BoundingBoxesGroups = [];
</script>

<p>
  <strong>Image:</strong>
  <%= @image_label.image %>
</p>

<%= render partial: "bbimage", locals: {image: @image_label.image, il: @image_label} %>

<p>
  <strong>Label:</strong>
  <%= @image_label.label %>
</p>

<p>
  <strong>Target:</strong>
  <%= @image_label.target %>
</p>

<%= link_to 'Edit', edit_image_label_path(@image_label) %> |
<%= link_to 'Back', image_labels_path %>
