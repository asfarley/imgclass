<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_label_set_id %><br>
    <%= f.text_field :image_label_set_id, :readonly => true %>
  </div>
  <div class="field">
    <%= f.label "Assigned worker:" %><br>
    <%= f.select :user_id, User.all.map{ |u| [u.email, u.id] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
