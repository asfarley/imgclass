<%= form_for(@label_set) do |f| %>
  <% if @label_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@label_set.errors.count, "error") %> prohibited this label_set from being saved:</h2>

      <ul>
      <% @label_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
